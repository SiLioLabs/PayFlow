{
  "name": "payflow-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Operational and analytics scripts for FlowPay",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "top-merchants": "ts-node top-merchants.ts",
    "deploy-pipeline": "ts-node deploy-pipeline.ts",
    "backup-indexer-db": "ts-node backup-indexer-db.ts"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^12.0.0",
    "better-sqlite3": "^9.4.3"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.8",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
